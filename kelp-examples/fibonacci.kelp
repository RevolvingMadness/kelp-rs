mcfn namespace:main {
    let n = 10

    let a = 0 to score
    let b = 1 to score
    let count = 0 to score

    while count < n {
        tellraw @a a
        
        (*a, *b) = (b, a + b)
        count += 1
    }

    tellraw @a a
}