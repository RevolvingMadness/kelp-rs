mcfn namespace:main {
    let i = 1 to data
    let i2 = 2 to data
    let f = 1.0 to data
    let f2 = 1.5 to data

    tellraw @a i == i  // 1
    tellraw @a i == i2 // 0
    tellraw @a f == f  // 1
    tellraw @a f == f2 // 0
}