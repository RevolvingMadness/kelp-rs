use std::*;

recursive runtime fn fibonacci(n: data<integer>) -> data<integer> {
    if n <= 1 {
        return n;
    }

    fibonacci(n - 1 to data) + fibonacci(n - 2 to data) to data
}

mcfn namespace:main {
    tellraw @a fibonacci(10 to data);
}
